<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.shijingfeng.wan_android">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络状态权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- WIFI状态权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 读取外部存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 写入外部存储权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 读取手机状态权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 读取设置权限 -->
    <uses-permission android:name="android.permission.READ_SETTINGS" />
    <!-- 写入设置权限 -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <!-- 挂载外部存储权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <!-- 硬件加速对X5视频播放非常重要，建议开启 -->
    <uses-permission android:name="android.permission.GET_TASKS" />

    <dist:module dist:instant="true" />

    <!--
    hasFragileUserData: 卸载APP时 是否保留 用户外部私有目录数据 (true: 保留  false: 不保留)
    networkSecurityConfig: Android 10 (API 29)以上默认仅支持 HTTPS 链接, 所以要想使用 HTTP 则需要此配置
    requestLegacyExternalStorage: 设置外部存储访问模式, 分为 Filtered View(默认) 和 Legacy View, Filtered View模式规定只能自由访问外部存储 APP 私有目录, 访问其他目录需要 MediaStore 或 SAF, Legacy View模式兼容以前版本, 无限制, 参考 https://mp.weixin.qq.com/s/aiDMyAfAZvaYIHuIMLAlcg
    -->
    <application
        android:name="com.shijingfeng.wan_android.WanAndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/WanAndroidTheme"
        android:hardwareAccelerated="true"
        android:hasFragileUserData="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute">

        <!-- 启动页 -->
        <activity android:name="com.shijingfeng.wan_android.ui.activity.SplashActivity"
            android:theme="@style/ProjectTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 登录 Activity -->
        <activity android:name="com.shijingfeng.wan_android.ui.activity.LoginActivity" />
        <!-- 注册 Activity -->
        <activity android:name="com.shijingfeng.wan_android.ui.activity.RegisterActivity" />
        <!-- 首页 Activity -->
        <activity android:name="com.shijingfeng.wan_android.ui.activity.MainActivity"
            android:launchMode="singleTask"/>
        <!-- 通用WebView页面 -->
        <activity android:name="com.shijingfeng.wan_android.ui.activity.WebViewActivity"
            android:configChanges="orientation|screenSize"/>
        <!-- 积分排行榜 Activity -->
        <activity android:name="com.shijingfeng.wan_android.ui.activity.CoinRankActivity" />
<!--        &lt;!&ndash; 积分记录 Activity &ndash;&gt;-->
<!--        <activity android:name="com.shijingfeng.wan_android.ui.activity.CoinRecordActivity" />-->
<!--        &lt;!&ndash; 我的收藏 Activity &ndash;&gt;-->
<!--        <activity android:name="com.shijingfeng.wan_android.ui.activity.ArticleCollectedListActivity" />-->
<!--        &lt;!&ndash; 知识体系 详细数据 Activity &ndash;&gt;-->
<!--        <activity android:name="com.shijingfeng.wan_android.ui.activity.KnowledgeClassifyDetailActivity" />-->

    </application>

</manifest>